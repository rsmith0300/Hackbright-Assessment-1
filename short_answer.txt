1) What does the `rm` command in the terminal do?

Your answer:
removes a file.

------------------
2) What terminal command would you use to list all the files and folder in the 
   current directory?

Your answer:
 ls

------------------
3) Explain the difference between these two commands in the terminal:
   
   $ cd Users
   $ cd /Users

Your answer:

cd users = opening a folder called Users in the current folder you are in.
cd /users = open folder called users in the root folder.

------------------
4) Describe what git is and what it is used for.

Your answer:
git is like an external hard to save your code/projects in.

------------------
5) Describe what GitHub is and what it is used for.

Your answer:

GitHub is a cloud saving drive where you save your code/prjects on the internet.

------------------
6) Which of these commands sets up a new git repository?
    a) git clone 
    b) git pull
    c) git init
    d) git status

Your answer:
   C
------------------
7) You want to check that you have properly added all the files you want to be
   tracked in git BEFORE you make a commit. Which command will do this?
     a) git push
     b) git commit
     c) git status
     d) git remote

Your answer:

   C
------------------
8) What is the difference between `git add` and `git commit`?

Your answer:

git add intiates git to track that file.
git commit saves the file to the git repository
------------------
9) What does the `git push` command do?

Your answer:

git push saves the file to gitHub.
------------------
10) When would you use the `git clone` command?

Your answer:
when you need to download saved projects from get github to your computer.

